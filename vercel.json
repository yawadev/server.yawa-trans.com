{ 
  "version": 2,
  "builds": [
    {
      "src":"src/main.ts",
      "user": "@vercel/node"
    }
   ],
  "routes": [
      {
        "src":"/{.*}",
        "dest": "src/main.ts",
        [
          "GET",
          "POST",
          "PUT",
          "DELETE"
        ],
      }
   ]
}
